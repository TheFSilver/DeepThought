
    <% if current_user?(@user) %>

        <%= link_to "Modifier mon compte", edit_user_path(current_user), class: 'btn btn btn-info' %>

        <%= link_to "Supprimer mon compte", @user, method: :delete,
                                    data: { confirm: "You sure?" }, class: 'btn btn-sm btn-danger'%>
    <% end %>
  </a>
